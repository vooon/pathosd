apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: pathosd-e2e
data:
  default.conf: |
    server {
        listen 80;
        location /healthz {
            return 200 '{"status":"ok"}';
            add_header Content-Type application/json;
        }
    }
